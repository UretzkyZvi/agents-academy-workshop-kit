# Signal Source Template

Use this before connecting Gmail, Calendar, CRM, forms, folders, or any other signal source.

```text
Signal source:
Owner:
Business workflow:

What event matters:

Filter rules:

Fields needed:

Fields forbidden:

Sensitive data involved:

How often to check:

Agent may:
- draft:
- summarize:
- classify:
- recommend:

Agent must not:

Human reviewer:

Approval required before:

Where approval is logged:

Failure behavior:

First test sample location:

Build / refine / stop decision after 3 samples:
```
